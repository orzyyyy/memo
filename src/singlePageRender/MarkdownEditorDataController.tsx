import React from 'react';
import ReactDOM from 'react-dom';
import MarkdownEditorDataController from '../controller/MarkdownEditorDataController';

ReactDOM.render(
  <MarkdownEditorDataController />,
  document.getElementById('root'),
);
